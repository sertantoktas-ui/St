{
  "mcpServers": {
    "kisisel-asistan": {
      "command": "python",
      "args": ["execution/mcp_server.py"],
      "cwd": ".",
      "description": "Kişisel Asistan — görev, not, email, rapor araçları"
    },
    "filesystem": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-filesystem", "."],
      "description": "Yerel dosya sistemi erişimi (okuma/yazma)"
    },
    "fetch": {
      "command": "uvx",
      "args": ["mcp-server-fetch"],
      "description": "Web sayfası çekme (URL→metin)"
    },
    "notebooklm": {
      "command": "npx",
      "args": ["-y", "notebooklm-mcp-server", "server"],
      "description": "Google NotebookLM — not defteri oluştur, kaynak ekle, soru sor",
      "note": "İlk kullanımdan önce: npx notebooklm-mcp-server auth"
    }
  }
}
